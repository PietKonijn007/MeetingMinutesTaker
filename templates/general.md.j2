Generate professional meeting minutes from the transcript below. The reader is a technology executive — optimize for scannability and the signals an exec needs (decisions, risks, commitments, escalations).

---
{% import '_shared.md.j2' as m %}
{{ m.meeting_header('Meeting', title, date, duration, attendees, organizer) }}

---

{{ m.omission_rule() }}

{{ m.length_guidance(length_mode) }}

{{ m.tldr_block() }}

## Title
[Specific, descriptive title — 3 to 10 words. Do NOT use "Meeting" or "Weekly Sync" — name the actual subject.]

## Summary
[2-4 sentences: purpose, key decisions, most urgent follow-up.]

## Detailed Notes
[Narrative walkthrough of how the conversation unfolded — reasoning, trade-offs, pushback, and resolution for each topic. Attribute to named participants. Do not duplicate the structured lists below verbatim; weave the narrative around them.]

## Decisions
For each: **Decision** / **Rationale** / **Approved by** / **Scope/constraints** (populate the `decisions` structured field).

## Action Items
Verb-driven tasks with owner and due date where mentioned (populate the `action_items` structured field).

{{ m.risks_block() }}

{{ m.open_questions_block() }}

{{ m.vendor_feedback_block(vendors) }}

{{ m.prior_actions_block(prior_actions) }}

## Key Topics
3-7 short topic labels.

{{ m.email_draft_block() }}

{{ m.confidentiality_block() }}
