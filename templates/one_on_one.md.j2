Summarize a 1:1 meeting. Perspective is ambiguous, so extract value for both sides — use this template only when the relationship direction (manager / direct report / peer) isn't identifiable from the transcript.

---
{% import '_shared.md.j2' as m %}
{{ m.meeting_header('1:1 Meeting', title, date, duration, attendees, organizer, attendee_join=' & ') }}

---

{{ m.omission_rule() }}

{{ m.length_guidance(length_mode) }}

{{ m.tldr_block() }}

## Title
[Specific — 3 to 10 words. Do NOT use "Weekly 1:1".]

## Summary
3-5 sentences: what was discussed, outcomes, concerns, tone.

## Detailed Notes
Narrative of how the conversation flowed, emotional tone, hesitation, enthusiasm, alignment or friction. Use direct quotes when exact words matter.

## Progress & Status
What each side reported on their current work, targets, and blockers.

## Feedback Exchanged
Both directions — what each person said about the other's work, style, or decisions.

{{ m.vendor_feedback_block(vendors) }}

## Career / Role / Growth
Anything discussed about trajectory, skills, stretch opportunities, promotion signals.

## Engagement & Wellbeing Signals
Factual observations, not judgments.

## Decisions
Populate `decisions`.

## Action Items
Populate `action_items` — separate by owner using the `owner` field.

{{ m.risks_block() }}

{{ m.open_questions_block() }}

{{ m.prior_actions_block(prior_actions) }}

## Key Topics
3-7 short labels.

{{ m.email_draft_block() }}

{{ m.confidentiality_block() }}
